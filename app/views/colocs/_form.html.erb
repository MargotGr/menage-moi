<div class="row">
    <div class="col-md-8 col-md-offset-2">
      <%= simple_form_for thing do |f| %>
        <%= f.input :name %>
        <%= f.button :submit, class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
