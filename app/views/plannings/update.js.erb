// Décommenter pour faire de l'AJAX

// var task = document.querySelector("[data-planning-id='<%= @planning.id %>']");
// task.classList.remove("card_icon_to_do");
// task.classList.add("card_icon_done");

// if (document.querySelectorAll(".card_icon_to_do").length == 0) {
//   document.querySelector("h1#match").insertAdjacentHTML("beforebegin", "<%= j render(partial: 'gain') %>");
//   var gainDiv = document.querySelector(".gain-div");
//   var closeBtnGain = document.querySelector("#gain.result-card i");
//   closeBtnGain.addEventListener("click", (event) => {
//     gainDiv.style.display = "none";
//   });
// }
